/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './public/**/*.{html,js}', // Scans all HTML and JS files in the public folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
